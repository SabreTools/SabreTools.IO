namespace SabreTools.Logging
{
    /// <summary>
    /// Severity of the logging statement
    /// </summary>
    public enum LogLevel
    {
        VERBOSE = 0,
        USER = 1,
        WARNING = 2,
        ERROR = 3,
    }
}
